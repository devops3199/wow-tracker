version: "3"
services:
  db:
    image: "mysql:latest"
  client:
    platform: linux/amd64
    build: ./wow-tracker-client
    ports:
      - "3000:3000"
  server:
    platform: linux/amd64
    build: ./wow-tracker-server
    ports:
      - "4000:4000"
